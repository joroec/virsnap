package vm

import "testing"

// TODO: add a minimal set of unit tests
/* func TestSum(t *testing.T) {
    total := 5 + 6
    if total != 10 {
       t.Errorf("Sum was incorrect, got: %d, want: %d.", total, 10)
    }
} */
